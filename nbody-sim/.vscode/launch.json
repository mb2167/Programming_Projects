{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run nbody-sim",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/nbody-sim",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "externalConsole": true,
      "MIMode": "gdb",
      "preLaunchTask": "cmake build"
    }
  ]
}
